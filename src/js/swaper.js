// Swaper start

new Swiper('.image-slider', {
    // Navigation
    navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev'
    },

    // Pagination
    pagination: {
        el: '.swiper-pagination',
        // Bullet
        clickable: true,
        // Dinamic bullet
        dinamicBullets: true, 
    },
});

// Swaper end
